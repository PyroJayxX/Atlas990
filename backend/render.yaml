# Exported from Render on 2026-06-01T08:04:03Z
version: "1"
projects:
- name: Atlas990
  environments:
  - name: Production
    services:
    - type: web
      name: Atlas990
      runtime: python
      repo: https://github.com/PyroJayxX/Atlas990
      plan: free
      region: singapore
      buildCommand: pip install -r requirements.txt
      startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
      autoDeployTrigger: commit
      rootDir: backend
      envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
